{
  "name": "i-love-text",
  "version": "1.0.0",
  "description": "Image-first Nepali OCR web app",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "echo Static frontend only - use Docker for full OCR API",
    "start": "serve frontend -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
